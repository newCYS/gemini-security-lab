name: Gemini Issue Triage
on:
  issues:
    types: [opened]

jobs:
  triage:
    runs-on: windows-latest
    permissions:
      contents: write
      issues: write
    steps:
      - name: Run Gemini CLI
        # 🚩 FIX: Use the specific 2026 stable tag
        uses: google-github-actions/run-gemini-cli@v0.32.0
        with:
          prompt: |
            Analyze and triage this issue:
            Title: "${{ github.event.issue.title }}"
            Body: "${{ github.event.issue.body }}"
          google_api_key: ${{ secrets.GEMINI_API_KEY }}
